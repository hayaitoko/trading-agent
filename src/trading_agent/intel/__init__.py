"""Agent-intelligence layer — tool envelope, turn context, and cost tracking.

This package provides the infrastructure for the ReAct-style tool-calling
trader loop introduced in WS-Agent A0.  It has no hard dependencies on the
rest of the package (no import cycles).
"""
